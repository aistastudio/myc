/**
 * Объявление для текстового импорта соседнего `schema.postgres.sql`:
 * `with { type: "text" }` вшивает файл строкой и в рантайме Bun, и при
 * `bun build --compile`, благодаря чему `myc serve --apply-schema` поднимает
 * базу сервера сам — контейнеру не нужны ни psql, ни копия схемы рядом.
 *
 * Имя файла — не прихоть: для импорта произвольного расширения TypeScript ждёт
 * объявление вида `<имя>.d.<расширение>.ts` (allowArbitraryExtensions), и
 * тогда оно видно КАЖДОМУ пакету, который компилирует импортирующий модуль, —
 * в отличие от ambient-объявления, которое видно только своему проекту.
 */
declare const text: string;
export default text;
