# W7: база знаний в интерфейсе (memory-b4f910fe7p03)

## Итог
Готово. Приёмка пройдена, оба гейта зелёные.

## Что уже стояло (найдено при разборе, не переделано)
- Путь записи целиком через `run()` из @myc/cli: `planCreateNote` собирает
  `myc remember` (очередь embed+absorb, attrs.source="agent"), `planCreate` —
  `myc create` (doc→document алиас, --repo), правка — `myc update`;
  `KIND_GAPS`/`FIELD_GAPS` отказывают громко там, где пути в CLI нет.
- `/api/kb` (kb.ts): список знаний с обеими осями охвата, читаемыми ядром
  (`readReach`/`readRepo`), счётчики подвала по всей базе.
- Клиент: вкладка «База знаний» — фильтры (виды, слои L0–L3, охват сессии),
  поиск, форма заведения, раскрытие строки с телом и связями + общий
  редактор свойств, честная метка «без охвата».

## Что добавлено в этой сессии
Третья ось — ярус (S41) — в интерфейсе не показывался вовсе:
- `workspace.ts`: `tierOf(dbPath, home)` — личный `~/.myc` против проектного
  `.myc/` по пути базы; граница префикса по каталогу (`.myc2` — не `.myc`).
- `server.ts`: `homeDir` в опциях (тесты личного яруса без трогания HOME),
  `tier` в `/api/boot` и на VizServer.
- `client/app.ts`: шапка показывает «ярус: личный (~/.myc)» / «ярус:
  проектный (.myc)» — один раз на базу, не примешано к осям строк.
- `kb.test.ts`: новый describe «ярус — ось открытой базы, а не строки»:
  проектный/личный boot на одной и той же базе, путь-граничные случаи,
  и guard `Object.keys(row)` без `tier` (несведение с S58/S59).

## Мутации — чем ловятся (kb.test.ts)
1. Заметка мимо `run()`: сравнение оплогов операция в операцию + assert
   очереди `["embed","absorb"]` + находимость поиском наравне с терминальной.
2. decision как отдельный вид: тест состава узла (kind='note',
   subtype='decision', card.type='note') краснеет.
3. Сведение охватов в одно поле: тест «строка несёт обе оси» — поля,
   счётчики и фильтры обеих осей независимы; посев читает каждую ось со
   своего ключа attrs.

## Гейты
- `bun test packages/web/` — 90 pass / 0 fail, 1841 expect(), ~8 c.
- `bun run typecheck` — все 13 пакетов exit 0.
- Полный прогон не запускался (по инструкции координатора).

## Границы
Тронуты только `packages/web/src/` и его тесты. `packages/cli/`,
`packages/core/`, `packages/retrieval/` не тронуты.
