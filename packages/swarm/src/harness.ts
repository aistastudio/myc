/**
 * ЕДИНЫЙ список харнессов — тех агентских CLI, которые myc обслуживает.
 *
 * До memory-7vywv63wma61 списков было два и они разъехались молча:
 * ростер знал `claude, opencode, kimi`, а `myc wire` ставил хуки для
 * `claude, codex, opencode`. Обе половины кусались. Попытку агента Codex
 * нельзя было записать («неизвестный харнесс "codex"»), хотя wire ему
 * конфиг уже поставил; Kimi числился в ростере, но wire его не настраивал
 * вовсе — агент под Kimi работал без скилла и без MCP.
 *
 * Поэтому список живёт ЗДЕСЬ, в самом нижнем пакете, которому он нужен
 * (swarm зависит только от @myc/core, а cli зависит от swarm), и все
 * остальные места обязаны читать его отсюда, а не повторять:
 *
 *   - ростер (roster.ts) — атрибуция попытки: чем задачу исполняли;
 *   - `myc wire` (cli/commands/wire.ts) — что ставить в проект;
 *   - `--agent` у absorb-session — кто позвал хук.
 *
 * Сторож — packages/cli/src/harness.wiring.test.ts: он ищет в исходниках
 * ЛЮБОЕ перечисление имён харнессов и краснеет на каждом, кроме этого файла
 * и замороженных DDL миграций. Разведёшь списки — упадёт.
 *
 * Добавление харнесса — это ВСЕГДА две правки: строка здесь и миграция роя,
 * расширяющая CHECK на swarm_model.harness / swarm_attempt.harness (текст
 * применённой миграции заморожен чек-суммой и правке не подлежит; см.
 * migrations/008-harness-codex.ts как образец перестройки таблицы).
 * Забудешь вторую — roster.test.ts («CHECK схемы принимает ровно
 * HARNESSES») покажет это на первом же прогоне.
 */
export const HARNESSES = ["claude", "codex", "opencode", "kimi"] as const;
export type Harness = (typeof HARNESSES)[number];
