// TODO(myc-fyq): implement absorb / supersede / decay and the L0→L1→L2→L3
// promotion pipeline (deterministic stage A first).
export type DistillStage = "absorb" | "supersede" | "decay" | "promote";
